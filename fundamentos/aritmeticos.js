const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d

const subtracao = a - b

const mul = a * b

const div = b / a

const modulo = b % 2

console.log(soma, subtracao, mul, div.toFixed(2), modulo)
